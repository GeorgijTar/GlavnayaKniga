<Window x:Class="GlavnayaKniga.WPF.Views.EmployeeTransferWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:GlavnayaKniga.WPF.Converters"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        Title="{Binding Title}" Height="370" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FontSize="14">

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Перевод сотрудника" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

        <!-- Новая должность -->
        <TextBlock Grid.Row="1" Text="Новая должность:" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <ComboBox Grid.Row="2" 
                  ItemsSource="{Binding Positions}"
                  DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedPosition}"
                  Margin="0,0,0,10"
                  Height="30"/>

        <!-- Дата перевода -->
        <TextBlock Grid.Row="3" Text="Дата перевода:" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <DatePicker Grid.Row="4" 
                    SelectedDate="{Binding TransferDate}"
                    Margin="0,0,0,10"
                    Height="30"
                    DisplayDateEnd="{x:Static system:DateTime.Today}"/>

        <!-- Номер приказа -->
        <TextBlock Grid.Row="5" Text="Номер приказа о переводе:" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <TextBox Grid.Row="6" 
                 Text="{Binding OrderNumber, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,20"
                 MaxLength="50"
                 Height="30"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Перевести" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding SaveCommand}"
                    IsDefault="True"
                    Margin="5"
                    MinWidth="90"
                    Height="35"
                    Background="#FF9800"/>
            <Button Content="Отмена" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"
                    Margin="5"
                    MinWidth="90"
                    Height="35"/>
        </StackPanel>

        <!-- Индикатор загрузки -->
        <Border Grid.RowSpan="8"
                Background="#80FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                CornerRadius="6">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10"/>
                <TextBlock Text="{Binding StatusMessage}" FontSize="14" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>